import React, { ReactElement } from "react";
import "./index.scss";
import Icon from "../Icon";

const AnimatedCheckmark: React.FC = (): ReactElement => {
  return <Icon name="checkmark-anim" />;
};

export default AnimatedCheckmark;
