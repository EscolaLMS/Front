name: Build and Deploy
on:
  push:
    branches:
      - master
      - main
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2.3.1

      - name: Install and Build 🔧 # This example project is built using npm and outputs the result to the 'build' folder. Replace with the commands required to build your project, or remove this step entirely if your site is pre-built.
        env:
          CI: false
          REACT_APP_PUBLIC_URL: https://demo-stage.escolalms.com/
          REACT_APP_YBUG_ID: 4a30b8sn4pfpdw7wp4c0
          REACT_APP_PUBLIC_API_URL: https://escola-lms-api.stage.etd24.pl
          REACT_APP_SENTRYDSN: https://88e772c161c249728708278186a4685b@sentry.etd24.pl/54
          REACT_APP_ROUTING_TYPE: HashRouter
        run: |
          npm i
          npm run build
      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@4.1.4
        with:
          branch: gh-pages # The branch the action should deploy to.
          folder: build # The folder the action should deploy.
